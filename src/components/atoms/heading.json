{
  "collectionName": "components_atoms_headings",
  "info": {
    "displayName": "Heading"
  },
  "options": {},
  "attributes": {
    "value": {
      "type": "string",
      "required": true
    },
    "variant": {
      "type": "enumeration",
      "enum": [
        "Title",
        "Sub-title"
      ]
    },
    "alignment": {
      "type": "enumeration",
      "enum": [
        "left",
        "center",
        "rigth"
      ]
    }
  }
}
